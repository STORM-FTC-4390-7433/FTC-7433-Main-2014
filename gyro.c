#ifndef GYROSYS_C
#define GYROSYS_C

/*task updateGyroSys(){
	updateDriveSys(drive, 100, 100);

	while(true) {
		wait1Msec(50);
		gyr.gyroscopeValue = SensorValue[S4]/20.;

		if (gyr.gyroscopeValue > 1) {
			while (gyr.gyroscopeValue > 1) {
				updateDriveSys(-50, 50);
			}
		}
		else if (gyr.gyroscope < -1) {
			while (gyr.gyroscope < -1) {
				updateDriveSys(50, -50);
			}
	  }

		updateDriveSys(100, 100);
	}
}
*/

	#endif
